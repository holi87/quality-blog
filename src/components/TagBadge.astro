---
// Deprecated in favour of Tag.astro — kept as a thin wrapper for backwards compat.
import Tag from './Tag.astro';

interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<Tag label={tag} variant="ghost" />
